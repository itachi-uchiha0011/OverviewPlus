version: "3.9"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - ENV=production
      - DEBUG=False
      - FRONTEND_URL=http://localhost:5000
      - APP_URL=http://localhost:5000
    restart: unless-stopped